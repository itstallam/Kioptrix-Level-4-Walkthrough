Now since we have gotten the password to john's account. We will try to 'ssh' into his machine.  Since we saw port 22 was open from
the previous nmap scan "nmap -p 0-65000 -A 192.168.56.15"

Command
-$ ssh -p 22 john@192.168.56.15 -oHostKeyAlgorithms=+ssh-rsa

You will be prompted say "yes"
The password is "MyNameIsJohn" from the previous SQLi Attack

See SSH.png
